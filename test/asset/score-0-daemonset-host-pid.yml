apiVersion: apps/v1
kind: DaemonSet
spec:
  selector:
    matchLabels:
      name: example-daemonset
  template:
    spec:
      hostPID: true
      containers:
        - name: example-daemonset
          image: example-daemonset:latest
