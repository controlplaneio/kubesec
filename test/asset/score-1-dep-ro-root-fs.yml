---
apiVersion: apps/v1
kind: Deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test
  template:
    spec:
      containers:
      - name: carts-db
        image: mongo
        securityContext:
          readOnlyRootFilesystem: true
